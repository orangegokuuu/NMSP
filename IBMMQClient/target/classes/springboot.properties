# Spring Boot main
spring.main.show_banner         = true
spring.pidfile                  = ${ibm.pid}

# Remote Shell setting
management.shell.ssh.port                  = ${mqclient.ssh.port}
management.shell.auth.simple.user.name     = ${mqclient.admin.user}
management.shell.auth.simple.user.password = ${mqclient.admin.pass}
management.shell.disabled-commands         = ${mqclient.admin.cmd.disable}

# Servlet Container setting
server.port                     = ${mqclient.servlet.port}
server.address                  = ${mqclient.servlet.address}
server.contextPath      		= ${mqclient.server.contextPath}

# HTTP Endpoint setting
security.user.name              = ${mqclient.admin.user}
security.user.password          = ${mqclient.admin.pass}
security.basic.path             = /manage/**
management.security.enabled     = true
management.security.role        = ADMIN
management.context-path         = ${mqclient.admin.context}
management.port                 = ${mqclient.manage.port}
management.address              = ${mqclient.manage.address}
security.ignored				   = /**

endpoints.shutdown.enabled      = true
endpoints.shutdown.sensitive    = true
endpoints.info.enabled          = true
endpoints.info.sensitive        = false
endpoints.health.enabled        = true
endpoints.health.sensitive      = true

spring.jackson.serialization.indent_output  = true