# Spring Boot main
spring.main.show_banner         = true
spring.pidfile                  = ${emg.pid}

# Remote Shell setting
management.shell.ssh.port                  = ${emg.ssh.port}
management.shell.auth.simple.user.name     = ${emg.admin.user}
management.shell.auth.simple.user.password = ${emg.admin.pass}
management.shell.disabled-commands         = ${emg.admin.cmd.disable}

# Servlet Container setting
server.port                     = ${emg.servlet.port}
server.address                  = ${emg.servlet.address}

# HTTP Endpoint setting
security.user.name              = ${emg.admin.user}
security.user.password          = ${emg.admin.pass}
security.basic.path             = /manage/**
management.security.enabled     = true
management.security.role        = ADMIN
management.context-path         = ${emg.admin.context}
management.port                 = ${emg.manage.port}
management.address              = ${emg.manage.address}

endpoints.shutdown.enabled      = true
endpoints.shutdown.sensitive    = true
endpoints.info.enabled          = true
endpoints.info.sensitive        = false
endpoints.health.enabled        = true
endpoints.health.sensitive      = true

spring.jackson.serialization.indent_output  = true
